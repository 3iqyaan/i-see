# I-See
A command-line tool to view and analyze files.
Now it only counts newline characters in a file.

## Usage
To use this tool, run the following command in your terminal:
```
cargo run
i-see.exe -f <file_name> [more file names...]
```
